#include <stdio.h>
void main(){
    int n,i,j;
    printf("Enter a number to check if it is prime number: ");
    scanf("%d", &n);
    for ( i = 1; i < 10 && i < n; i++)
    {
        
    }
    
    
}